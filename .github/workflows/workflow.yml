name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install FFMPEG
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Download Video
        run: curl "https://pixeldrain.dev/api/file/xqpvkdLu?download" -o video.mp4

      - name: Run Video Encoder
        run: python3 app.py

      - name: Video Uplaoder
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run:
          curl -T "video-e.mp4" -u :${API_KEY} https://pixeldrain.com/api/file/videooo.mp4